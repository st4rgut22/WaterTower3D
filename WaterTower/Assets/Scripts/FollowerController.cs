using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Follower : Person {



	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //only call if enemy is moving to a new tower
        //GameObject result = getNewTower();
        //if (result != null) Debug.Log("You have entered someone's square");
	}
}
